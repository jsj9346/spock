"""
Setup script for Quant Platform
"""
from setuptools import setup, find_packages

setup(
    name='quant-platform',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.11',
)
